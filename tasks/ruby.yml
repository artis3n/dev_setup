---

- name: Ruby | Set lib permissions
  become: yes
  ansible.builtin.file:
    path: '{{ item.name }}'
    recurse: yes
    owner: '{{ ansible_user }}'
    group: '{{ ansible_user }}'
    state: '{{ item.state }}'
  loop:
    - { name: '/var/lib/gems', state: 'directory' }
